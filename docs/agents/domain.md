# Domain docs

This is a single-context repository.

Before implementation work:

- Read `CONTEXT.md`.
- Read relevant decisions under `docs/adr/` when present.
- Use the project terminology defined by those documents.
- Surface conflicts with existing ADRs rather than silently overriding them.

Missing ADRs are not an error; proceed without creating speculative decisions.
