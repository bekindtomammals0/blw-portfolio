import type { PortfolioProject } from '../types/portfolio';

// Project content is introduced only after its working state, claims, and disclosure are verified.
export const projects = [] satisfies PortfolioProject[];
